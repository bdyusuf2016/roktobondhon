# Workspace Guidelines & Instructions

## Git Operations Rule
- **Strict Prohibition:** Do NOT execute `git pull` or `git push` without explicit instruction from the user.
- Always wait for the user's direct confirmation/request before syncing with any remote Git repository.
