# Git Workflow Rules

## Git Pull & Push Restrictions
- **Never perform `git pull` or `git push`** unless the user explicitly and directly instructs you to do so.
- Do not attempt background, automated, or pre-emptive pulls or pushes.
- Wait for explicit user confirmation and command before any remote git sync operations.
